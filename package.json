{
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "test:enrollment": "npm run cypress:run --spec 'cypress/tests/ui/enrollment.spec.js'"
  },
  "dependencies": {
    "cypress-real-events": "^1.7.1"
  },
  "devDependencies": {
    "cypress": "^10.2.0",
    "prettier": "^2.7.1"
  }
}
